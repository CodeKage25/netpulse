"""Reading meaning into the numbers.

Every module here takes a `Store` and a moment and returns a judgement: how good the
connection is, what is wrong with it, how much of the plan is left, where the delay
starts, what the history looks like outside this program. None of them poll anything,
and none of them know how the answer will be displayed.
"""
